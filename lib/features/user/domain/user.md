# Domain

Qui vivono le **regole di business** e i **tipi di dati puri**.  
Nessuna dipendenza da UI, API o framework.

- **entities**  
  Contiene le entità del dominio (modelli puri).

- **repositories**  
  Definisce i contratti che il layer data deve implementare.